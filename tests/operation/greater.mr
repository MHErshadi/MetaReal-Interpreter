a = []
c = 0

#* Test1 <int> type *#

v = 13 > 12
w = -4 > -4
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

v = 7 > 6.5
w = 9 > 9.01
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test3 *#

v = 1 > false
w = -2 > true
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test4 *#

v = 1000 > '%'
w = 10 > '8'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test5 <float> type *#

v = -9.5 > -10
w = 83.0 > 83
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test6 *#

v = 9.3 > 2.9
w = -12.3 > 3.1
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test7 *#

v = 1.5 > true
w = 0.1 > true
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test8 *#

v = 74.1 > 'J'
w = 75.0 > 'K'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test9 <bool> type *#

v = false > -1
w = true > 1
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test10 *#

v = true > 0.67
w = false > 0.01 
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test11 *#

v = true > false
w = false > false
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test12 *#

v = true > '\0'
w = true > '\\'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test13 <char> type *#

v = 'x' > 64
w = '!' > 34
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test14 *#

v = 'L' > 75.56
w = 'e' > 101.1
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test15 *#

v = '\t' > true
w = '\0' > false
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test16 *#

v = 'a' > 'A'
w = 'b' > 'b'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Result *#

if c == 16:
    "PASSED"
else:
    "       FAILED"
