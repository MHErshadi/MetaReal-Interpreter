r = []
c = 0

#* Test1 *#

if (object() is object()) === true && (object() is 1) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test2 *#

if (object() is object) === true && (object() is complex) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Result *#

if c === 2:
    "PASSED"
else:
    r
