a = []
c = 0

#* Test1 <str> type *#

if "Hello"[1] === 'e' && "Hello"[-1] === 'o'
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

try
{
    "Hello"[100]
    a += "FAILED"
}
except "Index out of range":
    try
    {
        "Hello"[-100]
        a += "FAILED"
    }
    except "Index out of range"
    {
        a += "PASSED"
        c++
    }

#* Test3 <list> type *#

if [1, 2.3, [1, '!']][2] === [1, '!'] && ["OK", "NO", "BAD"][-3] === "OK"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test4 *#

try
{
    [1, 2, 3][100]
    a += "FAILED"
}
except "Index out of range":
    try
    {
        [1, 2, 3][-100]
        a += "FAILED"
    }
    except "Index out of range"
    {
        a += "PASSED"
        c++
    }

#* Test5 <tuple> type *#

if (1, 2.3, (1, '!'))[2] === (1, '!') && ("OK", "NO", "BAD")[-3] === "OK"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test6 *#

try
{
    (1, 2, 3)[100]
    a += "FAILED"
}
except "Index out of range":
    try
    {
        (1, 2, 3)[-100]
        a += "FAILED"
    }
    except "Index out of range"
    {
        a += "PASSED"
        c++
    }

#* Result *#

if c === 6:
    "PASSED"
else:
    "       FAILED"
