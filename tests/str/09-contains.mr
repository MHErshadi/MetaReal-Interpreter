r = []
c = 0

#* Test1 *#

if ("home" in "sweet home") === true && ("Test" in "just some test") === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test2 *#

if ("d" in [51, object(), 3.78, 'd']) === true && ("k" in [107, 'K', "kkk", ["k"], ("k",)]) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test3 *#

if ("d" in (51, object(), 3.78, 'd')) === true && ("k" in (107, 'K', "kkk", ["k"], ("k",))) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Result *#

if c == 3:
    "PASSED"
else:
    r
