r = []
c = 0

#* Test1 *#

a = []
for i = 1.2 to 6:
    a += i

if a === [1.2, 2.2, 3.2, 4.2, 5.2] && i === 6.2
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test2 *#

a = []
for i = 2.3 to 8.9 step 2:
    a += i

if a === [2.3, 4.3, 6.3, 8.3] && i === 10.3
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test3 *#

a = []
for i = 1.5 to 3 step 0.75:
    a += i

if a === [1.5, 2.25] && i === 3.0
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test4 *#

a = []
for i = 2.1 to 6.3 step true:
    a += i

if a === [2.1, 3.1, 4.1, 5.1, 6.1] && i === 7.1
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test5 *#

a = []
for i = 0.1 to '(' step '\r':
    a += i

if a === [0.1, 13.1, 26.1, 39.1] && i === 52.1
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Result *#

if c === 5:
    "PASSED"
else:
    r
