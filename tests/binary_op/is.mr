a = []
c = 0

#* Test1 <none> type *#

if none is none && !(none is false)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 <object> type *#

if object() is object() && object() is object && !(object() is true) && !(object() is int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test3 <int> type *#

if 36 is 67 && 36 is int && !(36 is 36.0) && !(36 is float)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test4 <float> type *#

if 9.0 is 4.5 && 9.0 is float && !(9.0 is 9) && !(9.0 is int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test5 <complex> type *#

if 5i is (1 + 2i) && 5i is complex && !(5i is 5) && !(5i is float)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test6 <bool> type *#

if true is false && true is bool && !(true is 1) && !(true is int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test7 <char> type *#

if 'f' is 'p' && 'f' is char && !('f' is 102) && !('f' is int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test8 <str> type *#

if "k" is "Hello" && "k" is str && !("k" is 'k') && !("k" is char)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test9 <list> type *#

if [1, 2, 3] is [] && [1, 2, 3] is list && !([1, 2, 3] is (1, 2, 3)) && !([1, 2, 3] is tuple)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test10 <tuple> type *#

if (1, 2, 3) is (1,) && (1, 2, 3) is tuple && !((1, 2, 3) is [1, 2, 3]) && !((1, 2, 3) is list)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test11 <type> type *#

if int is int && int is 23 && !(int is float) && !(int is 12.3)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test12 <func> type *#

f1 = func (x, y):
    x * y

f2 = func (x, y, z):
    x * y * z

s1 = struct
{
    var int x
    var int y
}

s2 = struct
{
    var int x
    var int y
    var int z
}

if f1 is f2 && !(f1 is s1)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test13 <struct> type *#

if s1 is s2 && !(s1 is f1)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Result *#

if c == 13:
    "PASSED"
else:
    "       FAILED"
