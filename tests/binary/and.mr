a = []
c = 0

#* Test1 *#

v = true and true
w = false && true
if (v, w) are bool && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

v = object() and 12 and 1.5 and 6i and true and 'p' and [1, 2, 3] and (1, 2, 3) and int
w = none && 0 && 0.0 && 0i && false && '\0' && []
if (v, w) are bool && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test3 *#

v = false and not_defined
try
{
    w = true && not_defined
    a += "FAILED"
}
except "'not_defined' is not defined":
    if v is bool && !v
    {
        a += "PASSED"
        c++
    }
    else:
        a += "FAILED"

#* Result *#

if c == 3:
    "PASSED"
else:
    "       FAILED"
