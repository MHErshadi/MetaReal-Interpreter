a = []
c = 0

#* Test1 *#

v = true ? 732 : "OK"
w = false ? 732 : "OK"
if v === 732 && w === "OK"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

v = true ? : 'C'
w = false ? : 'C'
try
{
    v
    a += "FAILED"
}
except "'v' is not defined":
    if w === 'C'
    {
        a += "PASSED"
        c++
    }
    else:
        a += "FAILED"

#* Result *#

if c === 2:
    "PASSED"
else:
    "       FAILED"
