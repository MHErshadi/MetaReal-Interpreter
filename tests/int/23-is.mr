r = []
c = 0

#* Test1 *#

if (60 is 40) === true && (3 is 3.0) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Test2 *#

if (20 is int) === true && (5 is object) === false
{
    r += "PASSED"
    c++
}
else:
    r += "FAILED"

#* Result *#

if c === 2:
    "PASSED"
else:
    r
