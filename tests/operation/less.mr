a = []
c = 0

#* Test1 <int> type *#

v = 12 < 13
w = -4 < -4
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

v = 6 < 6.5
w = 10 < 9.99
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test3 *#

v = 0 < true
w = 1 < false
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test4 *#

v = 5 < '%'
w = 100 < '8'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test5 <float> type *#

v = -10.5 < -10
w = 83.0 < 83
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test6 *#

v = 1.3 < 2.9
w = 12.3 < -3.1
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test7 *#

v = 0.5 < true
w = 1.1 < true
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test8 *#

v = 73.9 < 'J'
w = 75.0 < 'K'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test9 <bool> type *#

v = false < 1
w = true < 1
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test10 *#

v = false < 0.67
w = true < 0.01 
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test11 *#

v = false < true
w = false < false
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test12 *#

v = true < '\\'
w = false < '\0'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test13 <char> type *#

v = 'x' < 121
w = '!' < 33
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test14 *#

v = 'L' < 76.56
w = 'e' < 100.9
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test15 *#

v = '\0' < true
w = '\b' < false
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test16 *#

v = 'A' < 'a'
w = 'b' < 'b'
if ((v, w) are bool) && v && !w
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Result *#

if c == 16:
    "PASSED"
else:
    "       FAILED"
