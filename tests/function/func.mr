func isPrime(x)
{
    if x < 2:
        return false

    for i = 2 to x ** 0.5 // 1 + 1:
        if not (x % i):
            return false

    return true
}

if isPrime(499) == true && isPrime(30) == false:
    "PASSED"
else:
    "       FAILED"
