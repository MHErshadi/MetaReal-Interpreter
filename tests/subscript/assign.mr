a = []
c = 0

#* Test1 <str> type *#

s = "whimper"
s[3] = 's'

if s == "whisper"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 *#

s = "waited"
s[2] += '\n'

if s == "wasted"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test3 *#

s = "new line: \t"
s[-1]++

if s == "new line: \n"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test4 <list> type *#

s = [1, 2, 2, 4]
s[-2] = 3

if s == [1, 2, 3, 4]
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test5 *#

s = [1, 2, [3, 4], 6]
s[2] += 5

if s == [1, 2, [3, 4, 5], 6]
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test6 *#

s = [2, 4, 4, 5]
s[1]--

if s == [2, 3, 4, 5]
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Result *#

if c == 6:
    "PASSED"
else:
    "       FAILED"
