a = []
c = 0

#* Test1 <list> type *#

if [1, 2, 3] are int && !((1, 2, 3.0) are int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Test2 <tuple> type *#

if (1, 2, 3) are int && !((1, 2, 3.0) are int)
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

#* Result *#

if c == 2:
    "PASSED"
else:
    "       FAILED"
