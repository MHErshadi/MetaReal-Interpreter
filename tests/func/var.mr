a = []
c = 0

#* Test1 *#

func f(x, y):
    "" + x + y

b = f

if b(1, 2) === "12"
{
    a += "PASSED"
    c++
}
else:
    a += "FAILED"

var int b = 40

#* Test2 *#

try
{
    b = f
    a += "FAILED"
}
except "Can not assign <func> to a type-specified variable (type is <int>)"
{
    a += "PASSED"
    c++
}

#* Test3 *#

var const b = 40

try
{
    b = f
    a += "FAILED"
}
except "'b' is a constant variable"
{
    a += "PASSED"
    c++
}

#* Result *#

if c === 3:
    "PASSED"
else:
    "       FAILED"
